using HarmonyLib;
using System;

namespace Heinermann.PerfOptimizations
{
  /*
  [HarmonyPatch(typeof(BrainScheduler), "RenderEveryTick", new Type[] { typeof(float) })]
  class BrainScheduler_RenderEveryTick
  {
    static bool Prefix(float dt)
    {
      return false;
    }
  }*/
}
